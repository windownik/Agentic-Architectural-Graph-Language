# Agentic-Architectural-Graph-Language
AAGL (Agentic Architectural Graph Language) — An LLM-native spatial data standard built specifically for autonomous AI agents. Deterministic graph architecture, integer-only millimeters (i64), and a token-optimized schema designed to replace legacy CAD formats.
